#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../pleeease/bin/pleeease-watch" "$@"
  ret=$?
else 
  node  "$basedir/../pleeease/bin/pleeease-watch" "$@"
  ret=$?
fi
exit $ret
